# Number-to-any-Base This program gives the representation of any number in any base. It was written in Borland C++ Builder and includes the source code plus the executable. It will run on any Widows version from XP up.
